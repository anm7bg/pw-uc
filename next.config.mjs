/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // domains: ["sanity.io", "via.placeholder.com"],
        domains: ["cdn.sanity.io", "via.placeholder.com"],
      },
};

export default nextConfig;
